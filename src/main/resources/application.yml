spring:
  application:
    name: UniCode

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: Nutella44!
    driver-class-name: org.postgresql.Driver

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: moviestarclient@gmail.com
    password: eenu rbsi obnl hzha
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080
  servlet:
    session:
      timeout: 86400
      cookie:
        name: JSESSIONID
        http-only: true
        secure: false
        same-site: lax

app:
  mail:
    from: moviestarclient@gmail.com

judge0:
  api-url: https://judge0-ce.p.rapidapi.com
  api-key: ${JUDGE0_API_KEY:YOUR_API_KEY_HERE}
